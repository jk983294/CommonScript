#!/usr/bin/env bash

cp file* ..                                 # * match zero or any characters
cp file? ..                                 # ? match one character
mkdir -p /a/b/{c,d}                         # make two directories