#!/bin/bash

wc somefile                             # lines words characters
wc -l somefile                          # only lines
wc -w somefile                          # only words
wc -c somefile                          # only characters
